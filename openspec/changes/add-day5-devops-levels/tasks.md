# Tasks: add-day5-devops-levels

- [ ] 在 `levels.ts` 中添加新的验证类型：`env_var_set`、`cron_exists`、`service_running`（如需要）
- [ ] 在 `Progress.tsx` 的 `chapterConfig` 中添加第 5 章配置（名称、颜色、图标）
- [ ] 在 `App.tsx` 的 `levels` 数组中追加 Level 31-40 的完整定义
- [ ] 在后端 `validator.ts` 中实现新验证类型的校验逻辑
- [ ] 确认 Docker 镜像中包含必要工具（rsync、logrotate、cron 等）
- [ ] 端到端测试：完成所有 10 个新关卡
