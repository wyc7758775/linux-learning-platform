# Tasks: add-day5-devops-levels

- [x] 在 `levels.ts` 中添加验证类型：`env_var_set`、`file_content_contains`
- [x] 在 `Progress.tsx` 的 `chapterConfig` 中添加第 5 章配置（DevOps 实战、紫色调、容器图标）
- [x] 在 `App.tsx` 的 `LEVELS` 数组中追加 Level 31-40（10 个 DevOps 关卡）
- [x] 确认后端验证器已支持所需验证类型（`output_contains`、`file_exists`、`file_permission`、`directory_exists`）
- [x] TypeScript 编译通过，前端正确显示第 5 章 10 个关卡
- [ ] 端到端测试：需要 Docker 容器环境支持（rsync、cron、logrotate、ssh-keygen 等工具预装）
